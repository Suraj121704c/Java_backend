public interface BicycleJi {

//    we can't define concrete methods in inside an interface even you can't define any instance variable.
    void applyBrake(int decrement);

    void speedUp(int increment);
}
